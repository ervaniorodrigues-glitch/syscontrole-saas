# Configuração do banco de dados PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=sua_senha_aqui
PGDATABASE=syscontrole_web
# Ou use a URL de conexão completa (recomendado para serviços cloud como Supabase)
# DATABASE_URL=postgres://usuario:senha@host:porta/banco
